# 食間改版：審查與驗收紀錄

## 基準與進度盤點

基準：`origin/main` 的 `8932b06`。原版已有 81 間餐廳、餐別／預算／料理篩選、轉盤、封鎖清單與結果分享。多人房間僅有 `js/room.js` 草稿，未載入，README 僅有名稱。

原版可重現的邏輯問題：未知時間視為營業中；跨夜只看當日；連續轉動的角度會累積前次落點偏移；轉動時可換清單；全不勾選時舊選項仍保留。本次以共用篩選、三態營業判斷、固定本輪選項及正確角度計算處理。

## 最終設計

品牌「食間」，定位為中正大學的日常選餐工具。依使用者指定，讀取 dessertProject 的現行 `app/globals.css`、`HeroScene.module.css`、`ShowcaseSection.module.css` 與 `CLAUDE.md` 作為視覺參考。主要採用紙色 `#F3EEE3`、柿色 `#AD5330`、深褐色 `#241D16`、在地宋體、大標與細線；沒有複製 dessertProject 的程式或素材。

保留靜態網站部署。移除執行期 Tailwind／Google Fonts 依賴，字型子集與授權置於 `assets/fonts/`。SVG 碗筷與品牌字標為本次原創。

## 審查入口

| 檔案                          | 核對重點                                                              |
| ----------------------------- | --------------------------------------------------------------------- |
| `js/core.js`                  | 台灣時區、跨夜歸屬、未知營業時間、複合篩選、儲存 ID 驗證              |
| `js/wheel.js`                 | 多次轉動仍命中同一目標扇形、動畫期間不可換選項、減少動態分支          |
| `js/app.js`                   | 清單與轉盤一致、收藏／排除／最近紀錄、儲存失敗、dialog 焦點、分享降級 |
| `index.html`、`css/style.css` | 桌面與手機比例、字型、鍵盤入口與小螢幕排版                            |
| `tests/*.test.cjs`            | 無第三方依賴的可重跑回歸檢查                                          |

## 已執行的檢查

`node --test tests/core.test.cjs tests/wheel.test.cjs`：7 個測試全數通過。

- 全部 81 筆資料的 ID 唯一性與既有時間格式。
- 台灣時間、分段時刻、含開始／不含結束。
- 跨夜、公休後凌晨、週日到週一、午夜及 24 小時。
- 未知與不合法時刻、損壞或錯誤型別的本機儲存值。
- 多條件交集、收藏、排除、待恢復清單與零結果。
- 連續五次指定落點、重入防護、轉動中換清單防護、減少動態立即完成。

另已執行 `node --check js/app.js`、`git diff --check`。

### 瀏覽器實際操作

使用 Codex 內建瀏覽器、本機 HTTP 伺服器，不是 Playwright E2E 套件自動測試。

| 流程                        | 觀察結果                                                                      |
| --------------------------- | ----------------------------------------------------------------------------- |
| 桌面首頁、轉盤、結果 dialog | 已看截圖並調整比例、字型及細節                                                |
| 390px 手機與 768px 平板     | 已看實際截圖；窄版採單欄                                                      |
| 320px 窄螢幕                | DOM scrollWidth 與 clientWidth 均為 305px，無水平捲動；修正小標及重設按鈕換行 |
| 晚餐＋神農路＋日式          | 清單與輪盤同為 1 間「丹飽處x味自慢」                                          |
| 單一選項抽選                | 立即顯示正確餐廳、詳細營業時段與地圖搜尋連結                                  |
| 收藏與重整                  | 重整後口袋名單仍保留該餐廳                                                    |
| 排除最後一間                | 轉盤停用，出現零結果狀態                                                      |
| 恢復排除店家                | 從「暫不考慮」逐間恢復成功                                                    |
| 正常抽選                    | 抽選時篩選與抽選按鈕均停用，結束後恢復並顯示結果                              |
| 分享                        | 顯示「已複製連結」；直接開啟 `?result=17` 顯示牛筋伯爵                        |
| 搜尋不存在的關鍵字          | 顯示空狀態，重設回到完整清單                                                  |
| 增載清單                    | 初始 12 張，增載後 24 張                                                      |
| Escape                      | 可關閉結果 dialog                                                             |

## 邊界與待使用者驗收

- 這不是即時餐廳服務，81 間店家的營業與價格仍是原有資料，沒有重新實地查證。
- 減少動態的邏輯有測試；未切換作業系統設定做真機動態驗收。
- 剪貼簿成功路徑有實際操作；拒絕權限的降級 UI 尚未用真實權限拒絕測試。
- 沒有宣稱通過 Safari、實體手機、螢幕閱讀器或 Lighthouse 全面稽核。
- 視覺已完成本機檢視，最終美感仍由使用者判斷。
- 使用者安排的 Claude Code 審查尚未執行，不將其他工具的核對當作該審查結果。

## 輔助文件核對

Antigravity 已完成 README／CLAUDE 與主要程式的靜態對照，唯一回報為 docs/review.md 不存在；主代理隨後以實際檔案確認該文件已存在。此核對不等於測試或 Claude Code 審查。OpenCode 因 Computer Use 無法辨識前景程序，未成功派送任務。
